import ProfilePage from "@/app/(withOutLayout)/(dashboard)/_components/profile/ManageProfile";

const page = () => {
  return (
    <div>
      <ProfilePage />
    </div>
  );
};

export default page;
