import React from 'react';
import LoginPage from '../_components/LoginList/LoginList';

const page = () => {
    return (
        <>
            <LoginPage/>
        </>
    );
};

export default page;